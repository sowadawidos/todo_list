import styled from '@emotion/native'

export const BottomSheetHeaderBox = styled.View({
    flexDirection: 'row',
    padding: 15,
    width: 300,
    display: 'flex',
    justifyContent: 'space-between',
})

export const BottomSheetHeaderClose = styled.TouchableOpacity({
    width: 20,
    height: 20,
    alignItems: 'center',
    justifyContent: 'center'
})
